# Cinema DDD CRUD - NestJS Study

API RESTful using NestJS with DDD, Clean Architecture, Prisma ORM (MySQL) and Swagger.

## Running

```bash
docker compose up db -d
npm install
npx prisma migrate dev
npm run start:dev
```

Swagger: http://localhost:3000/api/docs
